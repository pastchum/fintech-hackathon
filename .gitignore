# Python-specific ignores
*.pyc
*.pyo
*.pyd
__pycache__/
backend/*.env  # Environment files containing sensitive data (e.g., API keys)
backend/.env
*.env.*

# Virtual environment
venv/
fintech-env/

# IDE-specific ignores
.idea/
.vscode/
*.swp  # Swap files created by text editors like Vim
*.sublime-workspace
*.sublime-project

# Logs and debugging
logs/
*.log
*.out
*.err

# Test coverage
htmlcov/
.coverage
*.cover
.cache
.pytest_cache/

# ScrapeGraphAI cache or temporary files (if any; adjust as needed)
.scrapegraph/

# Docker
*.dockerignore
docker-compose.override.yml  # Local overrides that don't belong in the repo
.docker/
.docker-cache/

# FastAPI-related auto-generated files
*.sqlite3  # SQLite database files (if applicable)
instance/

# Ignore specific editor/system files
.DS_Store  # macOS
Thumbs.db  # Windows

# Node.js (if frontend exists in the repo)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env.local
.env.production
.env.development
